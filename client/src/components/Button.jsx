import React from "react";

const Button = () => {
  return (
    <button className="px-[80px] py-[15px] rounded-full border border-gray-400 cursor-pointer transition-all duration-300 hover:bg-black hover:text-white hover:scale-110">
      View All
    </button>
  );
};

export default Button;
